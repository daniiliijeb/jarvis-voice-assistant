# 🔄 Система автоматических обновлений

Jarvis поддерживает автоматическую проверку и установку обновлений через GitHub Releases.

## 🎯 Возможности

- ✅ Автоматическая проверка обновлений при старте (в фоне)
- ✅ Уведомление пользователя через TTS о доступном обновлении
- ✅ Обновление через голосовую команду "Джарвис обнови"
- ✅ Автоматическое резервное копирование перед обновлением
- ✅ Безопасная установка обновлений

## ⚙️ Настройка

### 1. Указать GitHub репозиторий

**Через переменную окружения (рекомендуется):**
```bash
set JARVIS_GITHUB_REPO=yourusername/jarvis-voice-assistant
```

**Или через конфигурацию:**
```python
# В jarvis/app/config.py
github_repo: Optional[str] = "yourusername/jarvis-voice-assistant"
```

### 2. Создать Release на GitHub

1. Перейдите в репозиторий на GitHub
2. Нажмите **"Releases"** → **"Create a new release"**
3. Укажите версию (например, `v0.1.1`)
4. Добавьте описание изменений
5. Прикрепите ZIP архив с проектом (опционально, можно использовать zipball)
6. Нажмите **"Publish release"**

## 📝 Версионирование

Версия указывается в файле `jarvis/data/version.txt`:
```
0.1.0
```

Формат версии: `X.Y.Z` (например, `0.1.0`, `1.2.3`)

## 🚀 Использование

### Автоматическая проверка

Jarvis автоматически проверяет обновления при старте (в фоновом режиме).

Если обновление доступно, Jarvis сообщит:
> "Сэр, доступно обновление до версии X.Y.Z. Скажите 'Джарвис обнови' для установки."

### Ручное обновление

Скажите Jarvis:
- **"Джарвис обнови"**
- **"Джарвис обновить"**
- **"Джарвис проверь обновления"**

Jarvis:
1. Проверит наличие обновлений
2. Скачает новую версию
3. Создаст резервную копию
4. Установит обновление
5. Сообщит о результате

## 🔧 Технические детали

### Модуль Updater

`jarvis/core/updater.py` содержит класс `Updater`:

- `check_for_updates()` - проверка обновлений через GitHub API
- `download_update()` - скачивание обновления
- `install_update()` - установка обновления с резервным копированием

### Процесс обновления

1. **Проверка версии** - сравнение текущей версии с последним Release
2. **Скачивание** - загрузка ZIP архива с GitHub
3. **Резервное копирование** - сохранение текущих файлов в `backup/`
4. **Установка** - распаковка и замена файлов (кроме `logs/`, `data/`, `.venv`)
5. **Обновление версии** - запись новой версии в `jarvis/data/version.txt`

### Безопасность

- Резервное копирование перед обновлением
- Сохранение пользовательских данных (`logs/`, `data/`)
- Исключение системных папок (`.venv`, `__pycache__`)
- Обработка ошибок с откатом

## 📋 Формат Release на GitHub

### Tag Name
Должен начинаться с `v` или не иметь префикса:
- `v0.1.1` ✅
- `0.1.1` ✅
- `release-0.1.1` ❌

### Assets (опционально)
Можно прикрепить ZIP архив с проектом:
- `jarvis-voice-assistant-v0.1.1.zip`
- Автоматически используется `zipball_url` если asset нет

### Release Notes
Описание изменений в Release будет показано пользователю при проверке обновлений.

## 🐛 Решение проблем

### Обновления не обнаруживаются

1. Проверьте, что указан правильный репозиторий:
   ```bash
   echo %JARVIS_GITHUB_REPO%
   ```

2. Проверьте, что Release создан на GitHub

3. Проверьте формат версии в Release tag

4. Проверьте логи: `jarvis/logs/jarvis.log`

### Ошибка при установке обновления

1. Проверьте подключение к интернету
2. Проверьте права доступа к файлам
3. Проверьте, что достаточно места на диске
4. Резервная копия сохранена в `backup/`

### Восстановление из резервной копии

Если обновление прошло неудачно:
```bash
# Скопируйте файлы из backup/ в корень проекта
xcopy backup\jarvis jarvis\ /E /I /Y
```

## 🔒 Безопасность

- ✅ Проверка через официальный GitHub API
- ✅ Скачивание только из указанного репозитория
- ✅ Валидация версий перед установкой
- ✅ Резервное копирование перед изменением файлов

---

**Система обновлений готова к использованию! 🎉**

