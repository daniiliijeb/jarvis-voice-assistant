# 👨‍💻 Инструкция для разработчика

## 📤 Как опубликовать проект на GitHub

### Шаг 1: Подготовка проекта

#### 1.1. Проверьте файлы

Убедитесь, что все важные файлы на месте:
- ✅ `INSTALLER.bat` - установщик
- ✅ `run.bat` - запуск
- ✅ `update.bat` - обновление
- ✅ `README.md` - документация
- ✅ `requirements-core.txt` - зависимости
- ✅ Все Python файлы в `jarvis/`
- ✅ `.gitignore` - исключает ненужные файлы

#### 1.2. Проверьте .gitignore

Убедитесь, что `.gitignore` содержит:
```
jarvis/data/elevenlabs_api_key.txt
jarvis/data/elevenlabs_voice_id.txt
jarvis/logs/*.log
.venv/
__pycache__/
```

#### 1.3. Удалите личные данные

**ВАЖНО!** Убедитесь, что в коде нет:
- ❌ Ваших личных API ключей
- ❌ Паролей
- ❌ Личных данных

### Шаг 2: Настройка репозитория на GitHub

#### 2.1. Создайте аккаунт (если нет)

1. Перейдите на https://github.com
2. Зарегистрируйтесь или войдите

#### 2.2. Создайте новый репозиторий

1. Нажмите **"+"** → **"New repository"**
2. **Repository name:** `jarvis-voice-assistant` (или другое имя)
3. **Description:** `Голосовой ассистент в стиле Jarvis из Iron Man для Windows`
4. Выберите **Public** (или Private, если хотите)
5. **НЕ** создавайте README, .gitignore, license (уже есть)
6. Нажмите **"Create repository"**

#### 2.3. Скопируйте URL репозитория

Например: `https://github.com/yourusername/jarvis-voice-assistant.git`

### Шаг 3: Инициализация Git

#### 3.1. Откройте терминал в папке проекта

```bash
cd D:\сс
```

#### 3.2. Инициализируйте Git

```bash
git init
```

#### 3.3. Добавьте все файлы

```bash
git add .
```

#### 3.4. Создайте первый коммит

```bash
git commit -m "Initial commit: Jarvis Voice Assistant v0.1.0"
```

### Шаг 4: Подключение к GitHub

#### 4.1. Добавьте remote

```bash
git remote add origin https://github.com/yourusername/jarvis-voice-assistant.git
```

**Замените `yourusername` на ваш GitHub username!**

#### 4.2. Переименуйте ветку в main

```bash
git branch -M main
```

#### 4.3. Отправьте код на GitHub

```bash
git push -u origin main
```

Если попросит авторизацию:
- Используйте Personal Access Token (не пароль)
- Получить токен: Settings → Developer settings → Personal access tokens → Generate new token

### Шаг 5: Настройка репозитория

#### 5.1. Добавьте описание

В настройках репозитория:
- **Website:** (если есть)
- **Topics:** `voice-assistant`, `jarvis`, `python`, `speech-recognition`, `tts`, `ai`, `windows`

#### 5.2. Настройте систему обновлений

**ВАЖНО!** Обновите конфигурацию для обновлений:

1. Откройте `jarvis/app/config.py`
2. Найдите строки:
   ```python
   github_repo_owner: str = "yourusername"
   github_repo_name: str = "jarvis-voice-assistant"
   ```
3. Замените на ваши данные:
   ```python
   github_repo_owner: str = "ваш_username"
   github_repo_name: str = "jarvis-voice-assistant"
   ```

4. Или используйте переменные окружения (рекомендуется):
   ```bash
   set GITHUB_REPO_OWNER=ваш_username
   set GITHUB_REPO_NAME=jarvis-voice-assistant
   ```

#### 5.3. Создайте первый Release

1. Перейдите в **Releases** → **"Create a new release"**
2. **Tag:** `v0.1.0` (должен совпадать с версией в `jarvis/data/version.txt`)
3. **Title:** `Jarvis Voice Assistant v0.1.0`
4. **Description:**
   ```
   Первый релиз Jarvis Voice Assistant
   
   Возможности:
   - Распознавание речи (Google STT / Whisper)
   - Синтез речи (ElevenLabs / pyttsx3)
   - Semantic Router для умного понимания команд
   - Context-Aware команды
   - Асинхронный TTS
   - Система автоматических обновлений
   ```
5. Нажмите **"Publish release"**

### Шаг 6: Проверка

#### 6.1. Проверьте, что всё работает

1. Клонируйте репозиторий в другую папку:
   ```bash
   cd C:\temp
   git clone https://github.com/yourusername/jarvis-voice-assistant.git
   cd jarvis-voice-assistant
   ```

2. Запустите установщик:
   ```bash
   INSTALLER.bat
   ```

3. Проверьте обновления:
   ```bash
   python -m jarvis.app.main --check-update
   ```

## 🔄 Как выпустить новую версию

### Шаг 1: Обновите версию

1. Откройте `jarvis/data/version.txt`
2. Измените версию (например, `0.2.0`)

### Шаг 2: Внесите изменения

Внесите все изменения в код, протестируйте.

### Шаг 3: Закоммитьте изменения

```bash
git add .
git commit -m "Release v0.2.0: Добавлены новые функции"
git push
```

### Шаг 4: Создайте Release на GitHub

1. Перейдите в **Releases** → **"Create a new release"**
2. **Tag:** `v0.2.0` (совпадает с версией!)
3. **Title:** `Jarvis Voice Assistant v0.2.0`
4. **Description:** Опишите что нового
5. Нажмите **"Publish release"**

### Шаг 5: Проверьте обновления

```bash
python -m jarvis.app.main --check-update
```

Должно показать новую версию!

## 📝 Важные моменты

### Версии

- Используйте семантическое версионирование: `0.1.0`, `0.2.0`, `1.0.0`
- Версия в `jarvis/data/version.txt` должна совпадать с тегом на GitHub
- Теги на GitHub могут быть с префиксом `v` (`v0.1.0`) или без (`0.1.0`)

### Релизы

- Создавайте Release для каждой новой версии
- GitHub автоматически создаёт ZIP архив
- Описание Release видно пользователям при обновлении

### Безопасность

- ❌ НЕ коммитьте API ключи
- ❌ НЕ коммитьте пароли
- ❌ НЕ коммитьте личные данные
- ✅ Используйте `.gitignore`
- ✅ Проверяйте файлы перед коммитом

## 🐛 Решение проблем

### Ошибка при push

```bash
# Если нужно авторизоваться
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### Обновления не работают

1. Проверьте, что репозиторий публичный
2. Проверьте, что есть хотя бы один Release
3. Проверьте, что версия совпадает с тегом
4. Проверьте логи: `jarvis/logs/jarvis.log`

---

**Готово! Теперь пользователи могут скачать и использовать ваш проект! 🚀**


