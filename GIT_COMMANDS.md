# 📤 Команды Git для обновления репозитория

## Если у вас УЖЕ есть репозиторий на GitHub

### Шаг 1: Проверьте текущий статус

```bash
git status
```

Покажет какие файлы изменены или добавлены.

### Шаг 2: Добавьте все новые файлы

```bash
git add .
```

Или добавьте конкретные файлы:

```bash
git add DEVELOPER_GUIDE.md
git add USER_GUIDE.md
git add INSTALLATION_GUIDE.md
git add START_HERE.md
git add UPDATES_INFO.md
git add update.bat
```

### Шаг 3: Создайте коммит

```bash
git commit -m "Добавлены инструкции для разработчика и пользователя, система обновлений"
```

Или более детальное описание:

```bash
git commit -m "Добавлены инструкции и система обновлений

- Добавлен DEVELOPER_GUIDE.md (инструкция для разработчика)
- Добавлен USER_GUIDE.md (инструкция для пользователя)
- Добавлен INSTALLATION_GUIDE.md (детальная установка)
- Добавлен START_HERE.md (быстрый старт)
- Добавлена система автоматических обновлений
- Добавлен update.bat для обновления
- Обновлен README.md со ссылками на инструкции"
```

### Шаг 4: Отправьте на GitHub

```bash
git push
```

Если это первый push в новую ветку:

```bash
git push -u origin main
```

## Полная последовательность команд

```bash
# 1. Проверьте статус
git status

# 2. Добавьте все файлы
git add .

# 3. Создайте коммит
git commit -m "Добавлены инструкции и система обновлений"

# 4. Отправьте на GitHub
git push
```

## Если нужно только обновить существующие файлы

Если файлы уже были загружены, но вы их изменили:

```bash
# Добавьте изменения
git add README.md
git add INSTALLER.bat
# или
git add .

# Закоммитьте
git commit -m "Обновлен README и установщик"

# Отправьте
git push
```

## Проверка после push

После `git push`:

1. Откройте ваш репозиторий на GitHub
2. Проверьте, что все файлы видны:
   - ✅ DEVELOPER_GUIDE.md
   - ✅ USER_GUIDE.md
   - ✅ INSTALLATION_GUIDE.md
   - ✅ START_HERE.md
   - ✅ UPDATES_INFO.md
   - ✅ update.bat

## Если возникли проблемы

### Ошибка: "remote origin already exists"

Если репозиторий уже настроен, просто выполните:
```bash
git push
```

### Ошибка авторизации

1. Используйте Personal Access Token вместо пароля
2. Получить токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
3. При push введите токен вместо пароля

### Ошибка: "nothing to commit"

Если Git говорит, что нечего коммитить:
1. Проверьте `git status` - возможно файлы не добавлены
2. Выполните `git add .` снова
3. Проверьте `.gitignore` - возможно файлы игнорируются

---

**Выполните эти команды и все новые файлы будут на GitHub! 🚀**


