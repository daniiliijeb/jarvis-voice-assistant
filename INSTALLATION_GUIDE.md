# 📥 Полная инструкция по установке

## Для пользователей (как скачать и установить)

### Способ 1: Скачать ZIP (самый простой)

1. **Перейдите на GitHub:**
   - Откройте браузер
   - Перейдите на: `https://github.com/yourusername/jarvis-voice-assistant`
   - (Замените `yourusername` на реальный username разработчика)

2. **Скачайте проект:**
   - Нажмите зелёную кнопку **"Code"**
   - Выберите **"Download ZIP"**
   - Сохраните файл на компьютер

3. **Распакуйте архив:**
   - Найдите скачанный файл (обычно в `Загрузки`)
   - Распакуйте в любую папку (например, `C:\Jarvis`)
   - Откройте распакованную папку

4. **Запустите установщик:**
   - Найдите файл **`INSTALLER_SIMPLE.bat`** (рекомендуется, работает на всех системах)
   - Или **`INSTALLER_NEW.bat`** (красивый интерфейс, может не работать на некоторых системах)
   - Или **`INSTALLER.bat`** (старая версия)
   - Запустите двойным кликом
   - Следуйте инструкциям на экране
   
   **⚠️ Если установщик не работает:** см. [РЕШЕНИЕ_ПРОБЛЕМ_УСТАНОВКИ.txt](РЕШЕНИЕ_ПРОБЛЕМ_УСТАНОВКИ.txt)

5. **Запустите Jarvis:**
   - После установки появится файл **`RUN_JARVIS.bat`**
   - Запустите его двойным кликом
   - Или запустите `run.bat`

### Способ 2: Через Git (для продвинутых)

1. **Установите Git:**
   - Скачайте с https://git-scm.com/download/win
   - Установите с настройками по умолчанию

2. **Откройте командную строку:**
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите Enter

3. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/yourusername/jarvis-voice-assistant.git
   cd jarvis-voice-assistant
   ```
   (Замените `yourusername` на реальный username)

4. **Запустите установщик:**
   ```bash
   INSTALLER_SIMPLE.bat
   ```
   (или `INSTALLER_NEW.bat` для красивого интерфейса, или `INSTALLER.bat` для старой версии)

5. **Запустите Jarvis:**
   ```bash
   run.bat
   ```

## Что делает установщик

**Рекомендуется использовать `INSTALLER_SIMPLE.bat`** - надежный установщик, работающий на всех системах Windows.

Альтернатива: `INSTALLER_NEW.bat` - красивый консольный установщик с прогресс-барами (может не работать на некоторых системах из-за кодировки).

`INSTALLER_NEW.bat` автоматически:

1. ✅ **Проверяет Python 3.11**
   - Если не найден, скачивает и устанавливает
   - Добавляет в PATH автоматически

2. ✅ **Создаёт виртуальное окружение**
   - Изолированная среда для зависимостей
   - Не конфликтует с другими проектами

3. ✅ **Устанавливает зависимости**
   - SpeechRecognition (распознавание речи)
   - PyAudio (работа с микрофоном)
   - pyttsx3 (синтез речи)
   - ElevenLabs (премиум голос)
   - faster-whisper (офлайн STT)
   - sentence-transformers (Semantic Router)
   - pygetwindow и pyautogui (Context-Aware)
   - И другие...

4. ✅ **Настраивает проект**
   - Создает необходимые папки
   - Настраивает обновления из GitHub
   - Настраивает ElevenLabs (опционально)

4. ✅ **Настраивает Jarvis**
   - Создаёт необходимые папки
   - Настраивает конфигурацию
   - Запрашивает API ключ ElevenLabs (опционально)

5. ✅ **Создаёт ярлык запуска**
   - `RUN_JARVIS.bat` для удобного запуска

## Требования

- **Windows 10/11**
- **Интернет** (для установки зависимостей и Google STT)
- **Микрофон** (для голосовых команд)
- **~500 МБ свободного места** (для установки)

## Первый запуск

1. **Проверьте систему:**
   ```bash
   python -m jarvis.app.main --health
   ```
   Должно показать статус всех компонентов.

2. **Запустите Jarvis:**
   ```bash
   run.bat
   ```

3. **Дождитесь приветствия:**
   > "Привет, сэр. Джарвис онлайн."

4. **Скажите:**
   > "Джарвис"
   
   Jarvis ответит:
   > "Да, сэр"

5. **Попробуйте команду:**
   > "открой браузер"

## Настройка (опционально)

### ElevenLabs для реалистичного голоса

1. Получите API ключ:
   - Зарегистрируйтесь на https://elevenlabs.io
   - Перейдите в Settings → API Keys
   - Создайте новый ключ

2. Сохраните ключ:
   - Откройте файл `jarvis\data\elevenlabs_api_key.txt`
   - Вставьте ключ
   - Сохраните

3. (Опционально) Выберите голос:
   - Откройте `jarvis\data\elevenlabs_voice_id.txt`
   - Вставьте ID голоса (например, `pNInz6obpgDQGcFmaJgB` для Adam)
   - Сохраните

### Whisper STT (офлайн распознавание)

1. Установите faster-whisper:
   ```bash
   pip install faster-whisper
   ```

2. Переключите STT:
   ```bash
   set STT_ENGINE=whisper
   ```

## Решение проблем

### Ошибка: "Python 3.11 not found"

**Решение:**
1. Установите Python 3.11:
   - Скачайте с https://www.python.org/downloads/
   - При установке выберите **"Add Python to PATH"**
2. Перезапустите установщик

### Ошибка: "Could not install dependencies"

**Решение:**
1. Проверьте интернет соединение
2. Запустите установщик от имени администратора
3. Попробуйте установить вручную:
   ```bash
   pip install -r requirements-core.txt
   ```

### Микрофон не работает

**Решение:**
1. Проверьте права доступа:
   - Settings → Privacy → Microphone
   - Включите доступ для приложений
2. Проверьте систему:
   ```bash
   python -m jarvis.app.main --health
   ```

### Команды не распознаются

**Решение:**
1. Говорите четче и громче
2. Проверьте интернет (для Google STT)
3. Попробуйте Whisper STT (офлайн)

---

**Готово! Наслаждайтесь использованием Jarvis! 🚀**


